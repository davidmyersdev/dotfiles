#!/usr/bin/env bash

for package_dir in ./packages/*
do
  package=$(basename ${package_dir})

  ./bin/stow-package "$package"

  if [ -d "${package_dir}/__sudo__" ]
  then
    sudo ./bin/stow-package "$package"
  fi
done
