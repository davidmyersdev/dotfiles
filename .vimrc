set nocompatible " disable vi compatibility
set number " show line numbers
set expandtab " insert spaces instead of tabs
set smarttab " indent to match context
set tabstop=2 " number of spaces to use for a tab char
set softtabstop=2 " number of spaces to move when using tab or backspace keys
set shiftwidth=2 " number of spaces to insert in place of a tab
set backspace=indent,eol,start " allow backspace to erase indentation, newlines, etc

filetype off
syntax on
