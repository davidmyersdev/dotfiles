#!/usr/bin/env bash

# All paths are relative to the repo root.
stow --dir ./packages/caddy --target $(brew --prefix)/etc --stow resources

if command -v brew > /dev/null
then
  # Start the caddy service.
  brew services start caddy
fi

# Make sure the root cert is trusted.
caddy trust
